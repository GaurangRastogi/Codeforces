#include<bits/stdc++.h>
using namespace std;
typedef long long int ll;
//tutorial
void solve(){
    ll n;
    cin>>n;
    vector<ll>s(n+1,0),c(n+1,0);
    for(int i=1;i<=n;i++)
        cin>>s[i];
    for(int i=1;i<=n;i++)
        cin>>c[i];

    int mn=c[0];
    for(int i)
}

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    solve();
    return 0;
}