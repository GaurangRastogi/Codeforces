#include <bits/stdc++.h>
using namespace std;

typedef long long int ll;

ll memoization(vector<ll>&n,ll ind,ll flag,ll count){
    if()
}
int main() {
    ll n;
    cin>>n;

    vector<ll>v(n);

    for(auto &it:v)
        cin>>it;
    
    // vector<vector<pair<ll,ll>>>dp(n,vector<pair<ll,ll>>(n,{-1,-1}));


}